# ReAct

- Цель: улучшить контракт эндпоинта POST /api/reviews, добавив валидацию входа, лимит размера diff и обработку ошибок LLM.
- Доступные входы: только practices/practice_01/TRAINING_PR.diff и practices/practice_01/CASE.md (правила SEC-1, API-1, REL-1, OUT-1, SCOPE-1, QA-1, OBS-1).
- Разрешённые действия: читать TRAINING_PR.diff, читать CASE.md, предлагать минимальные изменения в app/api.py и app/review_service.py, формулировать проверки результата.
- Запрещённые действия: approve/merge/edit репозитория, использование внешних источников, изменение правил CASE.md, добавление аутентификации, перевода на async, DI-рефакторинга, биллинга.
- Максимальное число шагов: 5.
- Условие остановки и вопроса человеку: если требуются данные вне TRAINING_PR.diff/CASE.md или изменения за пределами app/api.py и app/review_service.py — остановиться и спросить у человека, можно ли расширить входы или область изменений.

## Запрос

Ты — инженер, применяющий ReAct (Reason + Act) к улучшению слабого артефакта Практики 1. Цель: улучшить контракт POST /api/reviews, добавив валидацию входа, лимит размера diff и обработку ошибок LLM.

Контекст (Context Pack):
- SEC-1: перед отправкой во внешний LLM из diff удаляются секреты;
- API-1: diff длиннее 20 000 символов отклоняется с HTTP 413;
- REL-1: таймаут внешнего LLM 10 секунд; ошибка превращается в контролируемый ответ;
- OUT-1: ответ содержит необходимые поля; в нашем случае — {"comment": str};
- SCOPE-1: сервис только советует; не пишет код в GitHub;
- QA-1: риск включается, только если подтверждён diff или правилом;
- OBS-1: в логах только request_id, длительность, статус; содержимое не логируется;
- Решение всегда принимает человек.

Доступные входы: practices/practice_01/TRAINING_PR.diff, practices/practice_01/CASE.md. Никаких внешних источников.

Разрешённые действия (Action):
- Read(TRAINING_PR.diff)
- Read(CASE.md)
- ProposePatch(app/api.py, app/review_service.py)
- FormulateChecks()

Запрещённые действия: approve/merge/edit репозитория, изменение CASE.md, добавление аутентификации, async, DI-рефакторинга, биллинга, использование веба.

Работай по циклу не более 5 шагов:

Thought: опиши следующий логичный шаг на основе цели и наблюдений.
Action: выбери одно из {Read(TRAINING_PR.diff) | Read(CASE.md) | ProposePatch(app/api.py, app/review_service.py) | FormulateChecks()} и выполни его.
Observation: кратко зафиксируй, что обнаружено или предложено.

Повторяй Thought -> Action -> Observation максимум 5 раз. Если для прогресса нужны данные вне TRAINING_PR.diff/CASE.md или изменения за пределами app/api.py и app/review_service.py — остановись и задай вопрос человеку.

Финальный ответ:
1) Минимальный патч к app/api.py и app/review_service.py, который:
   - вводит Pydantic-модели ReviewRequest/ReviewResponse и указывает response_model у эндпоинта;
   - ограничивает размер входного diff до MAX_DIFF_CHARS = 20_000, при превышении возвращает HTTP 413;
   - оборачивает вызов llm.generate(...) в try/except, маппит ошибку в 502 с понятным detail;
   - оставляет обработчик синхронным и контракт ответа {"comment": str} по OUT-1;
2) Набор проверок (пошаговые):
   - POST /api/reviews c пустым телом {} -> 422;
   - POST /api/reviews c diff длиной > 20_000 -> 413;
   - если мок LLM бросает исключение -> 502 и понятный detail, без утечек prompt/секретов по SEC-1;
   - валидный diff -> 200 и {"comment": "..."};
   - в /docs присутствуют схемы запроса/ответа для эндпоинта.

## Наблюдаемые действия и результаты

| Шаг | Действие | Наблюдение | Решение продолжить или остановиться |
|---|---|---|---|
| 1 | Read(TRAINING_PR.diff) | Найден небезопасный доступ к полю тела запроса: TRAINING_PR.diff: app/api.py L35-38 — `def create_review(payload: dict) -> dict[str, str]: return review_service.review(payload["diff"])`. Риск KeyError и отсутствие контракта. | Продолжить: требуется валидация входа и схема запроса/ответа. |
| 2 | Read(TRAINING_PR.diff) | Отсутствует ограничение размера и обработка ошибок LLM: TRAINING_PR.diff: app/review_service.py L19-22, на L21 `answer = self.llm.generate(prompt)` без try/except и таймаута. Нет SEC-1-фильтра, нет маппинга ошибок. | Продолжить: предложить модели и обработку с кодами 413/502. |
| 3 | ProposePatch(app/api.py, app/review_service.py) | Предложить Pydantic-модели ReviewRequest/ReviewResponse и указать `response_model` у эндпоинта для документации и 422 на невалидном теле. | Продолжить: добавить обработку ошибок LLM и лимит diff. |
| 4 | ProposePatch(app/api.py, app/review_service.py) | Обернуть `llm.generate(...)` в try/except с маппингом исключений на 502; ввести `MAX_DIFF_CHARS = 20_000` и возвращать 413 при превышении; оставить обработчик синхронным. | Продолжить: сформулировать проверки. |
| 5 | FormulateChecks() | Сформулированы проверки: {} -> 422; diff > 20_000 -> 413; исключение LLM -> 502; валидный запрос -> 200 {"comment": ...}; /docs содержит схемы. | Остановиться: цель достигнута, внешних данных не требуется. |

## Что изменили в исходном артефакте

- Файл и раздел: app/api.py (create_review), app/review_service.py (ReviewService.review).
- Изменение: введена валидация через Pydantic-модель запроса/ответа и `response_model`, добавлен лимит на размер diff, вокруг вызова LLM добавлен try/except с маппингом ошибки в 502.
- Как проверили: POST {} -> 422; diff > 20 000 -> 413; мок LLM кидает исключение -> 502; валидный diff -> 200 {"comment": …}; в /docs отображаются схемы запроса/ответа.
- Что отклонили: async-обработчик, DI-рефакторинг, аутентификацию и rate limiting.

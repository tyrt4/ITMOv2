# E2E-проверки

| Сценарий пользователя | Предусловия | Действие | Наблюдаемый результат | Evidence |
|---|---|---|---|---|
| Позитивный | Сервис запущен; LLM отвечает без ошибок; `diff` корректен и ≤ 20 000 символов | Отправить `POST /api/reviews` с телом `{ "diff": "<корректный небольшой diff>" }` | 200 OK; тело содержит `summary`; `risks` (≤3, каждый с `file`, `line`, `evidence`, `risk`); `checks` | CASE.md: OUT-1; TRAINING_PR.diff: app/api.py L35-38; app/review_service.py L19-22 — текущее несоответствие формату `OUT-1` |
| Негативный | `diff` отсутствует в теле запроса | Отправить `POST /api/reviews` с `{}` | 422 Unprocessable Entity (валидация входа); обработчик не падает с 500 | TRAINING_PR.diff: app/api.py L35-38 — `payload["diff"]` без валидации (текущее поведение: KeyError → 500); ожидаемое — 422 по контракту ввода |
| Граничный | Лимит длины `diff` согласно API-1 | A: `len(diff)=20000` → POST `/api/reviews`; B: `len(diff)=20001` → POST `/api/reviews` | A: 200 OK; B: 413 Payload Too Large; вызов LLM не происходит | CASE.md: API-1; TRAINING_PR.diff: app/api.py L35-38 — сейчас нет проверки размера |

## Как использовали AI

- Для чего: оформить сквозные пользовательские сценарии (позитивный, негативный, граничный) и ожидаемые результаты по OUT-1 и API-1.
- Тип промпта: master prompt (QA‑инженер)
- Строка в [`prompts.md`](prompts.md): P1-10 — Сквозные сценарии по TRAINING_PR.diff
- Что проверили и исправили сами: привязали сценарии к правилам OUT-1 и API-1 и строкам из diff; зафиксировали несоответствие текущей реализации и целевого контракта для 422 и формата ответа; не добавляли шагов вне артефактов
